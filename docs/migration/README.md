# Migration

- `prototype-inventory.csv` is the per-file source of migration disposition.
- `prototype-inventory.md` records classification rules totals and validation.
- `documentation-status.md` records authority duplicates gaps and normalization
  recommendations.

The prototype is reference input only. Reuse requires individual review and a
signed commit that updates the relevant inventory row.
