# Operations

Start with `local-development.md`. Deployment runbooks incident response backup
restore and production SLO documents are required before a production launch but
are outside this foundation batch.

Use `opportunity-discovery-runbook.md` for Batch 2 source policy, rate limit,
circuit, schema drift, checkpoint, reconciliation, outbox, and retention incidents.
